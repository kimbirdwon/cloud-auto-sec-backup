---
- name: "K3s 인프라 구성"
  hosts: k3s_cluster  # deploy.yml에 정의된 것과 통일
  become: true        # sudo 권한 사용
  gather_facts: true  # 시스템 전체 정보 수집 활성화
  become_method: sudo  
  become_user: root    
  roles:
    - common
    - k3s
    - helm
    - monitoring
    - nginx_waf
